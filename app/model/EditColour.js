//Initialise the data design model for the store
//Initialise the data design model for the store
Ext.define("StudentApp.model.EditColour", {
	extend: "Ext.data.Model",
	config: {
		fields: [
			{name: "colour", type: "string"}
		]
	}
});